# Token para publicar no VS Code Marketplace (PAT do Azure DevOps).
# Copie para .env e preencha: cp .env.example .env
# Ou defina no ambiente: $env:VS_MARKETPLACE_TOKEN="seu_token"
VS_MARKETPLACE_TOKEN=
